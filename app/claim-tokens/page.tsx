import { TestnetTokenClaim } from "@/components/TestnetTokenClaim";

export const metadata = {
  title: "Claim test tokens | Riska"
};

export default function ClaimTokensPage() {
  return <TestnetTokenClaim />;
}
